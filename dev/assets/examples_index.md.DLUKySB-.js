import{_ as a,o as s,c as i,ao as t}from"./chunks/framework.B9--V88m.js";const g=JSON.parse('{"title":"Examples","description":"","frontmatter":{},"headers":[],"relativePath":"examples/index.md","filePath":"examples/index.md","lastUpdated":1781596949000}'),n={name:"examples/index.md"};function l(r,e,o,d,h,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Examples" tabindex="-1">Examples <a class="header-anchor" href="#Examples" aria-label="Permalink to &quot;Examples {#Examples}&quot;">​</a></h1><p>juliac compiles functions from disk, so the example functions live in the <code>MexicahExamples</code> package (<code>examples/src/MexicahExamples.jl</code>) rather than in a script&#39;s <code>Main</code> — a <code>Main</code>-defined function cannot be compiled. Each <code>examples/&lt;name&gt;.jl</code> is a thin driver that builds a subset of those functions, and each has a matching documentation page.</p><h2 id="Lean,-verified-examples" tabindex="-1">Lean, verified examples <a class="header-anchor" href="#Lean,-verified-examples" aria-label="Permalink to &quot;Lean, verified examples {#Lean,-verified-examples}&quot;">​</a></h2><p>These build into a real MEX with <code>--trim=safe</code>, exercised end to end in CI:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Example</th><th style="text-align:right;">What it demonstrates</th></tr></thead><tbody><tr><td style="text-align:right;"><a href="./scalar">Scalar addition</a></td><td style="text-align:right;">Hello-world: two <code>Float64</code> arguments, one output</td></tr><tr><td style="text-align:right;"><a href="./matrix">Matrix scaling</a></td><td style="text-align:right;">Zero-copy <code>Matrix{Float64}</code> input, one-copy output</td></tr><tr><td style="text-align:right;"><a href="./sparse">Sparse Frobenius norm</a></td><td style="text-align:right;"><code>SparseMatrixCSC</code> marshaling</td></tr><tr><td style="text-align:right;"><a href="./linalg">LinearAlgebra</a></td><td style="text-align:right;">SVD, solve, det/inv, and handle-based LU solvers</td></tr><tr><td style="text-align:right;"><a href="./handles">Opaque handles</a></td><td style="text-align:right;">Persist Julia structs across MEX calls via <code>UInt64</code> registry keys</td></tr></tbody></table><p>Build any of them (instantiate the environment once):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=examples</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate()&#39;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # once</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=examples</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/scalar_add.jl</span></span></code></pre></div><h2 id="Experimental-extensions" tabindex="-1">Experimental extensions <a class="header-anchor" href="#Experimental-extensions" aria-label="Permalink to &quot;Experimental extensions {#Experimental-extensions}&quot;">​</a></h2><p>Mexicah ships package extensions for <strong>DataFrames</strong>, <strong>Enzyme</strong>, <strong>ForwardDiff</strong>, <strong>JuMP</strong>, <strong>ModelingToolkit</strong>, and <strong>CUDA</strong>. They are <strong>experimental</strong>: each pulls in a large framework whose runtime is fundamentally dynamic, so a MEX that uses one <strong>does not compile under <code>--trim=safe</code></strong> (verified — Enzyme&#39;s <code>autodiff</code>, JuMP&#39;s MOI layer, DataFrames&#39; internals, and MTK/DiffEq&#39;s solvers all emit unresolved dynamic calls). The same applies to <code>DifferentialEquations.jl</code>. There are deliberately <strong>no example pages</strong> for them, because every example here is one that actually compiles and runs.</p><p>If you need one of these frameworks, load it in your environment and build with <code>trim=false</code> — you get a large, working (un-trimmed) MEX. For an ODE, the trim-safe route is to write the right-hand side and a fixed-step integrator as plain Julia (no framework), which compiles cleanly like the examples above.</p><h3 id="ModelingToolkit" tabindex="-1">ModelingToolkit <a class="header-anchor" href="#ModelingToolkit" aria-label="Permalink to &quot;ModelingToolkit {#ModelingToolkit}&quot;">​</a></h3><p><code>build_mex_from_mtk(sys)</code> (in <code>MexicahMTKExt</code>) generates the right-hand side and Jacobian of a simplified <code>ODESystem</code> as <code>f(u, p, t)</code> / <code>J(u, p, t)</code>. The <code>(u, p, t)</code> callables are numerically correct: <code>u</code> is in <code>unknowns(sys)</code> order and <code>p</code> (a flat vector of parameter values) in <code>parameters(sys)</code> order — <code>structural_simplify</code> reorders both — and <code>p</code> is assembled into the <code>MTKParameters</code> object current MTK requires (passing a flat vector straight through silently produced wrong numbers in older versions). The generated functions use <code>RuntimeGeneratedFunction</code>s and pull MTK into the runtime, so the result is <code>trim=false</code>. Producing a fully juliac-compiled MEX additionally needs the generated closures emitted into an importable on-disk package (they currently live in the extension module, which juliac cannot <code>import</code>) — a known limitation.</p><h3 id="Enzyme-/-ForwardDiff-gradients-trimfalse-recipe" tabindex="-1">Enzyme / ForwardDiff gradients (<code>trim=false</code> recipe) <a class="header-anchor" href="#Enzyme-/-ForwardDiff-gradients-trimfalse-recipe" aria-label="Permalink to &quot;Enzyme / ForwardDiff gradients (\`trim=false\` recipe) {#Enzyme-/-ForwardDiff-gradients-trimfalse-recipe}&quot;">​</a></h3><p><code>@mexgradient</code> compiles the gradient of a scalar loss <code>f(x::Vector{Float64})::Float64</code> into a MEX <code>[g] = f_grad(x)</code>. Enzyme is not trim-safe, so pass <code>trim=false</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Grad</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Mexicah, Enzyme          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># loading Enzyme activates the backend</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">loss</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(abs2, x)         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># scalar-valued, one Vector{Float64} argument</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@mexgradient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> loss backend</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:enzyme</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> trim</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><div class="language-matlab vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">matlab</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">run(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mex/mexicah_setup.m&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">loss_grad([</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% → [2 4 6]</span></span></code></pre></div><p><code>trim=false</code> yields a large but working (un-trimmed) MEX that bundles Enzyme&#39;s AD runtime. <code>backend=:forwarddiff</code> can build trim-safe for small inputs (omit <code>trim</code>). This is the general pattern for any experimental extension: load the framework and pass <code>trim=false</code>.</p>`,17)])])}const k=a(n,[["render",l]]);export{g as __pageData,k as default};
